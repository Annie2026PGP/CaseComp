# Cases

One folder per case competition, created by asking Claude Code to "start a
new case for <competition name>" (see the `new-case` skill). Each gets:

```
cases/<case-slug>/
  brief.md
  research/
  analysis/
  deck/
  submission/
```

Nothing here yet — this folder fills up as competitions come in.
