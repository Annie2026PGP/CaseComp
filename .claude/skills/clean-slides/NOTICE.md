Vendored from: https://github.com/tmustier/clean-slides
Commit: c67a56a31cba898eb6e76314f80d6b3301d71975 (2026-05-07)
License: MIT (see LICENSE in this folder)

This is a pip-installable Python CLI (`pptx` command), not a pure-markdown
skill — see "Setup" in SKILL.md. Not vendored: `.github/` (CI), `tests/`,
`typings/`, `.pre-commit-config.yaml` (dev-only tooling not needed at
runtime), and `examples/custom-template/*.pptx` (large binary samples;
the package's own bundled `example-template.pptx` is kept).
